@{
    ViewData["Title"] = "Підтвердження прив'язування телефону";
    Layout = "_AccountLayout";
}

<div class="card">
    <div class="card-body flex flex-col gap-5 p-10">
        <div class="text-center mb-2.5">
            <h3 class="text-lg font-medium text-gray-900 leading-none mb-2.5">
                Підтвердіть номер телефону
            </h3>
            @if (TempData["PendingPhoneNumber"] != null)
            {
                <p class="text-sm text-gray-600">
                    Код відправлено на телефон @TempData["PendingPhoneNumber"]
                </p>

                <div class="bg-blue-100 border border-blue-300 p-3 rounded mt-2">
                    <p class="text-sm text-blue-800">
                        <i class="bi bi-info-circle me-1"></i>
                        Ви прив'язуєте телефон до існуючого акаунта
                    </p>
                </div>

                <div class="bg-yellow-100 border border-yellow-300 p-3 rounded mt-2">
                    <p class="text-sm text-yellow-800">
                        <strong>Для тесту:</strong> Перевірте консоль логів для отримання коду
                    </p>
                </div>
            }
        </div>

        @if (TempData["ErrorMessage"] != null)
        {
            <div class="bg-red-600 text-white p-3 rounded-lg mb-4 border border-red-700">
                <p class="m-0 font-medium">@TempData["ErrorMessage"]</p>
            </div>
        }

        <form asp-action="VerifyPhoneLink" method="post">
            <input type="hidden" name="phoneNumber" value="@TempData["PendingPhoneNumber"]" />
            @Html.AntiForgeryToken()

            <div class="flex flex-col gap-1 mb-4">
                <label class="form-label font-normal text-gray-900">
                    6-значний код підтвердження
                </label>
                <input class="input text-center text-xl tracking-widest"
                       name="code"
                       value=""
                       placeholder="000000"
                       type="text"
                       maxlength="6"
                       required />
                <small class="text-xs text-gray-500 mt-1">
                    Введіть 6-значний код з SMS для прив'язування телефону
                </small>
            </div>

            <button type="submit" class="btn btn-primary flex justify-center w-full mb-2">
                <i class="bi bi-check-circle me-2"></i> Підтвердити прив'язування
            </button>

            <div class="text-center">
                <a class="text-sm link" href="/Account/RegisterWithPhone">
                    Повернутись до реєстрації
                </a>
            </div>
        </form>
    </div>
</div>