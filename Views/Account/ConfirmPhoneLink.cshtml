@{
    ViewData["Title"] = "Підтвердити прив'язування телефону";
    Layout = "_AccountLayout";
}

<div class="card">
    <div class="card-body flex flex-col gap-5 p-10">
        <h3 class="text-lg font-medium text-gray-900 leading-none mb-2.5">
            Підтвердити прив'язування телефону
        </h3>

        <div class="bg-blue-50 border border-blue-200 p-4 rounded mb-4">
            <div class="flex items-start">
                <i class="bi bi-phone text-blue-500 mt-0.5 mr-3"></i>
                <div>
                    <p class="font-medium text-blue-800 mb-1">
                        Знайдено існуючий акаунт
                    </p>
                    <p class="text-blue-700 mb-1">
                        Email: <strong>@ViewBag.LinkPhoneEmail</strong>
                    </p>
                    <p class="text-blue-700 mb-1">
                        Телефон: <strong>@ViewBag.LinkPhonePhone</strong>
                    </p>
                    <p class="text-blue-700">
                        Бажаєте прив'язати цей телефон до вашого акаунта?
                    </p>
                </div>
            </div>
        </div>

        <div class="flex gap-3">
            <form method="post" asp-action="ConfirmPhoneLink">
                @Html.AntiForgeryToken()
                <input type="hidden" name="confirm" value="yes" />
                <button type="submit" class="btn btn-primary flex items-center">
                    <i class="bi bi-check-circle mr-2"></i> Так, прив'язати телефон
                </button>
            </form>

            <form method="post" asp-action="ConfirmPhoneLink">
                @Html.AntiForgeryToken()
                <input type="hidden" name="confirm" value="no" />
                <button type="submit" class="btn btn-outline-secondary flex items-center">
                    <i class="bi bi-x-circle mr-2"></i> Ні, скасувати
                </button>
            </form>
        </div>

        <p class="text-sm text-gray-500">
            <i class="bi bi-info-circle mr-1"></i>
            Після прив'язування телефону ви зможете входити через SMS-код.
        </p>
    </div>
</div>